{
  "name": "@survey-tracker/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend serverless para monitoreo de encuestas Zoho",
  "main": "api/get-counts.js",
  "scripts": {
    "dev:vercel": "vercel dev",
    "build": "echo 'Build handled by Vercel'",
    "lint": "eslint . --ext .js",
    "test": "vitest run",
    "type-check": "echo 'skipped - JavaScript serverless endpoints'",
    "clean": "rm -rf .vercel node_modules"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "eslint": "^8.57.0",
    "vercel": "^32.0.0",
    "vitest": "^1.6.0"
  }
}
